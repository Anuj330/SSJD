# Base URL for the backend API.
# Leave EMPTY in production behind nginx (relative URLs are proxied to the API).
# For local `npm run dev`, Vite proxies /api etc. to localhost:8000, so empty is fine too.
VITE_API_BASE_URL=
